# Images No Alt Fixture

Two images below. The first has alt text but points at a missing file —
so `repo_resolved = 0` collapses its V_scaffold to zero. The second has
alt text and a repo-resolvable target.

![broken](./missing-image.png)

![alt here](code_fences.md "title")
